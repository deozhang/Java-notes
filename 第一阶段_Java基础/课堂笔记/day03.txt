回顾：
1.运算符和表达式 ：算术运算符（+-*/%  ++  --）
		 关系运算符（> < >= <= == != 返回结果是布尔类型）
		逻辑运算符（&& ||  ！注意  单双的区别）
		拓展赋值运算符（=   +=  -= ......）
		连接符（"+",可以连接字符串，也可以连接字符串与其他数			据类型）
2.三目运算（条件运算）
	结构: boolean 表达式？表达式1：表达式2;
	三目运算可以嵌套使用
3.分支结构:
if语句：
	if语句：很多情况，但是只写自己需求一种的情况，满足情况，执行		代码
	if-else:整个事件，只有两种情况，两者必然执行其一（互斥关系）
	if--else if：整个事件有多种情况，将每种情况按照条件执行，只		执行一种情况后，整个语句结束
		（互斥关系）
switch-case：特殊的分支结构
	switch条件句中在jdk1.6版本以下（包括1.6）支持整型和字符类型
	jdk1.6版本以上支持字符串类型（字符和整型）
        case句是判断是否与switch语句中的变量值相等，如果相等则执行该
		case句，
        switch-case:一般和break搭配使用，达到跳出switch结构的作用
今天：


循环:          
while:  

do-while:        

for: 
 

	





































































		